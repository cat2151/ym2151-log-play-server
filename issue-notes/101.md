# issue 最終段でのevent listのデータ形式は、addr data pairとする #101
[issues #101](https://github.com/cat2151/ym2151-log-play-server/issues/101)

# これまでの形式は？
- addrだけ、dataだけ
- それぞれでsample単位の時刻を指定
# これからは？
- addr dataのpair
- 前提
    - 最終段で、同一時刻レジスタ書き込み時の
        - 2sample delayを入れる、
        - が実装済みであること
        - 別issueを参照
- なのでevent list時はまだ、
    - 「同一時刻レジスタ書き込みeventが並んでいる状態」
    - とする
# メリット
- ※2sample delayを最終段、のissueも参照のこと
- シンプル
    - data形式がシンプルになる
# close条件
- 最終段でのevent listのデータ形式が、addr data pairとなること
- 「addr data pairから、addr行とpair行を作っていた処理」が、削除されること
